
<project name="SafeService" default="deploy.service">

      
    <property name="base.dir" value="${project.base.dir}\feedHostObject\positive\mashup788\service"/>
    <property name="MASHUP_HOME" value="${mashup.envt.MASHUP_HOME}"/>

       
    <target name="deploy.service">
        
     <copy file="${base.dir}/src/rssFeed1.js" tofile="${MASHUP_HOME}/scripts/samples/mashup788.js"
              overwrite="true"/>
    </target>

    
</project>
