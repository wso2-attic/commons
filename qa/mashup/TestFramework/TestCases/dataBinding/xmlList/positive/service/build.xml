
<project name="SafeService" default="deploy.service">

      
    <property name="base.dir" value="${project.base.dir}\dataBinding\xmlList\positive\service"/>
    <property name="MASHUP_HOME" value="${mashup.envt.MASHUP_HOME}"/>

       
    <target name="deploy.service">
        
     <copy file="${base.dir}/src/schemaTest.js" tofile="${MASHUP_HOME}/scripts/samples/mashup757.js"
              overwrite="true"/>
    </target>

    
</project>
