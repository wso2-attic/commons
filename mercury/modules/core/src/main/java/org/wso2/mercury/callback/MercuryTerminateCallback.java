package org.wso2.mercury.callback;

/**
 * this interface is used to keep set a call back to
 * indicate the termination of the sequence
 */

public interface MercuryTerminateCallback {
     public void onComplete();
}
